<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yang.zhengce.dao.CityPolicyDao">
  <select id="findCity" parameterType="string" resultType="cityPolicy">
      select * from city where city=#{city}
  </select>

    <select id="findAllCity" resultType="city">
        select * from city
    </select>

</mapper>